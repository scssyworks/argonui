class #component# {
  constructor({ el }) {
    this.root = el;
  }
  cache = {};
  initCache() {
    /* Initialize selector cache here */
    /**
     * If component is added more than once please use "this.root" hook to prevent multiple bindings.
     * Example:
     * this.cache.$submitBtn = this.root.querySelector('.js-submit-btn');
     */
  }
  bindEvents() {
    /* Bind Events here */
    /**
     * Example:
     * const { $submitBtn } = this.cache;
     * $submitBtn.addEventListener('click', () => { ... });
     */
  }
  init() {
    /* Mandatory method */
    this.initCache();
    this.bindEvents();
  }
}

export default #component#;
